#include <iostream>
#include <cmath>
using namespace std;
int factorial(int a);
int main()
{
	int n;
	cin >> n;
	cout<<factorial(n);

	system("pause>nul");
	return 0;
}
int factorial(int a)
{
	int sum = 1;
	for (int i = 1; i <= a; i++)
		sum *= i;
	return sum;
}
